{
  "name": "exits-saas-lms",
  "version": "1.0.0",
  "description": "ExITS SaaS LMS - API, Web, and Mobile Applications",
  "private": true,
  "scripts": {
    "dev:api": "cd api && npm run start:dev",
    "dev:web": "cd web && npm run dev",
    "dev:mobile": "cd loanflow && npm start",
    "dev:all": "echo 'Run dev servers individually: npm run dev:api, npm run dev:web, npm run dev:mobile'",
    "build:api": "cd api && npm run build",
    "build:web": "cd web && npm run build",
    "build:mobile": "cd loanflow && npm run build",
    "install:all": "cd api && npm install && cd ../web && npm install && cd ../loanflow && npm install",
    "db:migrate": "cd api && npm run db:migrate",
    "db:seed": "cd api && npm run db:seed",
    "db:rollback": "cd api && npm run db:migrate:rollback"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
